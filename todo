TASKS [*=done / -=not yet]

    [*] install all project packages React NextJs, with testing for the API connection between
        /client and /server directory

        [*] make endpoint called /login contain a functionaly of login form with fancy design by checking
            if username and password are right the default gonna be [admin/admin] thos value will be stored
            in the file itself without connection with databse until we make the database
            and design the Login form with fancy design using pure css with black dark theme background and box shadow for the whole login box and make and modern inputs animation !
    [-] configure database by install MariaDB in a docker container using docker composer
        then fill databse with random and check the connection between An API, and fetch data        
        in the web page interface(react)
